# Backend API base URL used by the frontend at runtime.
# Must point to the publicly reachable backend API (no trailing slash needed).
PUBLIC_API_URL=http://localhost:8080/api

# Public origin of the frontend (used at build time to prerender sitemap.xml
# and robots.txt, and for canonical/OG URLs at runtime).
# PUBLIC_SITE_URL=https://example.com
